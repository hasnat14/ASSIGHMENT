let usernames=["admin","ahsan21","hasnat12","awais.1","haris333"]

for(let i=0;i<usernames.length;++i){
    if( usernames[i]==="admin"){console.log( "Hello admin, would you like to see a status report");
    }else{console.log( `hello ${usernames}thank you for logging in again`);
    }
}
